# TradeProof — web UI

Static pages served by the FastAPI app (`uvicorn app.main:app --port 8080`).

Open http://127.0.0.1:8080/

**Educational / research only** — not investment advice. Live market data from free public feeds may be delayed or incomplete.
